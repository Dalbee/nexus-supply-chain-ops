name: 'nexus_supply_chain_ops'  # Changed - to _
version: '1.0.0'
config-version: 2

profile: 'nexus_supply_chain_ops'  # Changed from 'default'

model-paths: ["models"]
macro-paths: ["macros"]

# THIS IS THE KEY: Global variable override
vars:
  catalog: workspace

models:
  nexus_supply_chain_ops:
    +catalog: workspace
    +schema: supply_chain_analytics

tests:
  nexus_supply_chain_ops:
    +catalog: workspace
    +schema: supply_chain_analytics